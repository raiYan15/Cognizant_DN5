# Student Course Portal — Hands-On 1 Task 1 Duplicate

## Based on Student-course-portal Task Mapping

This task reflects the initial Angular project setup and app shell from the Student-course-portal sample.

## Files referenced

- `angular.json` — workspace build and serve configuration
- `package.json` — scripts and dependencies
- `tsconfig.json` / `tsconfig.app.json` / `tsconfig.spec.json` — TypeScript configuration
- `src/main.ts` — bootstraps the root app component
- `src/app/app.config.ts` — standalone app providers and router setup
- `src/app/app.ts` — root component layout
- `src/index.html` — HTML host page

## Task summary

- Initialize or inspect the Angular project configuration
- Confirm the app is bootstrapped through `main.ts`
- Review standalone app setup in `app.config.ts`
- Verify the root component and host page structure
